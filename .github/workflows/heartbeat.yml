name: Heartbeat Test

on:
  push:
    branches:
      - main

  schedule:
    - cron: "*/5 * * * *"

jobs:
  heartbeat:
    runs-on: ubuntu-latest

    steps:
      - name: Heartbeat
        run: |
          echo "trigger works"
          date
