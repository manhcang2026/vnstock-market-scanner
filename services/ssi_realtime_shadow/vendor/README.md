# SSI SDK local vendor folder

Do **not** commit the SSI FCData SDK archive into this public repository.

For Docker builds, copy the official archive downloaded from SSI into this folder locally, for example:

```text
vendor/ssi_fc_data-2.2.2.tar.gz
```

`.gitignore` excludes `.tar.gz` and `.whl` files in this folder.
